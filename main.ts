import { Plugin } from 'obsidian';

/**
 * 年度ごとに日記を比較表示するObsidianプラグインの初期セットアップ
 */
export default class YearlyDiaryComparatorPlugin extends Plugin {
	async onload() {
		// 今後の機能追加のための初期セットアップのみ
	}

	onunload() {
		// クリーンアップ処理（必要に応じて実装）
	}
}
